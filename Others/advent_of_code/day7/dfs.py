'''
def numworkers(workers))
    flag_zero = False
    while not flag_zero:
        minimum = min(workers)
        remove minimum from all in workers
        go through all in in list if any is zero then
            flag_zero = True

    return workers

def part2():
    if number of accessible_nodes == 1:
        time += time_for_node

    free_workers = look how many workers that are not busy
    removed_nodes = []
    for worker in min(free workers, accessible nodes)
        remove minnode
        removed_nodes.append(minnode)
        update worker_time.index(0) with time for minnode

    workers = numworkers(workers)












'''